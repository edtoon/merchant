{
  "name": "gg-common",
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf ./dist && NODE_ENV=production babel --copy-files ./src --out-dir dist",
    "test": "standard dist"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-latest": "^6.24.0",
    "babel-preset-stage-0": "^6.22.0",
    "standard": "^9.0.0"
  }
}
